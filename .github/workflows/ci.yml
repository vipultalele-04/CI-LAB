name: Build the CI job for nodejs application
on:
  push:
    branches:
      - ci-cd-branch
  pull_request:
    branches:
      - main


jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: list all the files and folder in build directory
        run: ls 

      - name: Upload artifacts check the node js application path 
        uses: actions/upload-artifact@v4
        with:
          name: nodejs-app-artifacts
          path: ./

      - name: get the user_name and password from github
        #run: echo "Username: ${{ vari.USERNAME }} and Password: ${{ secrets.PASSWORD }}"
        run: |
          echo "Username: ${{ vars.USER_NAME }}"
          echo "Password: ${{ secrets.PASSWORD }}"

